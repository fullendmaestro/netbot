# netbot
netbot is an IT infrastructure assistant that enables engineers monitor, manage, and automate it network infrastructure. 

### Local ssh test:
**On windows**:

```powershell
Start-Service sshd
ssh
```

Add a device with ip address 127.0.0.1